export enum HandlerContextEventId {
    UPDATE_USERS = "updateUsers",
    SET_LOADING = "setLoading",
    RESET = "reset",
}

export enum SocketContextEventId {
    SET_ISCONNECTED = "setisConnected",
}

export enum AuthContextEventId {
    CHECK_TOKEN_STATUS = "checkTokenStatus"
}